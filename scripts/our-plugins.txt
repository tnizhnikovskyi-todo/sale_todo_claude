# Плагіни фази продажів — межа дії наших перевірок.
#
# Навіщо файл: у спільному репозиторії методології поруч лежать плагіни інших фаз
# (ініціація, дискавері, ТР, білд). Наші перевірки вимагають структури SKILL.md,
# яку тримає тільки ця фаза, тому глоб `plugins/*` там дав би червоне CI на чужих
# скілах у першому ж пуші. Кожна перевірка читає цей перелік і не виходить за нього.
#
# Каталог, у якому лежать плагіни, задається змінною PLUGDIR (типово `plugins`) —
# щоб при переносі не правити глоби, а виставити одну змінну.
odoo19-sales-prep
odoo19-sales-demo
odoo19-sales-offer
