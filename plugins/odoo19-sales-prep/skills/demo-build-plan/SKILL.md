---
name: demo-build-plan
description: >
  Запускай на стадії Qualified, коли вирішено, що демо потрібне, і треба описати, що
  саме буде налаштовано в демо-базі — до того, як її почали збирати. Пише план збірки
  мовою менеджера, без термінів Odoo: які процеси покажемо, на яких даних, які ролі,
  і що свідомо НЕ показуємо. Сейл читає і встигає заперечити, поки база ще не зібрана.
  Тригерні фрази — "склади план демо-збірки", "що покажемо на демо", "постановка
  на демо", "план збірки демо", "що налаштувати в демо". НЕ збирає саму базу — це
  demo-base-builder; НЕ сценарій проведення — його пише demo-runbook після збірки.
  Результат — `план_демо-збірки_[назва_компанії].md`.
---

# Demo Build Plan

## Мета

Зафіксувати домовленість про зміст демо **до** збірки, а не після. Збірка бази —
найдорожчий крок фази; переробляти її тому, що сейл побачив не те, чого очікував,
не можна.

Тому документ пишеться мовою менеджера: сейл мусить прочитати і зрозуміти, **що
побачить клієнт**, не знаючи назв модулів Odoo.

**Регістр виходу: 2 — внутрішній людський.** Читають сейл і той, хто збирає базу.
Клієнту не надсилається.

---

## Інструменти та середовище

**Читання вхідних файлів:**
- `журнал_продажу_[компанія].md` → `Read`. Беремо `G-N` (що доведеться покривати),
  `D-N` (домовленості), `X-N` (що виключено), відкриті `Q-N`, `P-N` (обіцянки —
  їх часто доводиться показувати)
- `повнота_даних_[компанія].md`, `попередня_оцінка_[компанія].md` → `Read` за потреби
- `підготовка_до_брифу_[компанія].md` → `Read`: галузева довідка дає мову і типовий процес
- `питання_клієнта_[компанія].md` → `Read` **якщо існує**. Це коло 2: пакет від
  `client-questions-pack` після першого демо. Тоді план збірки будується **під ці питання**,
  а не з нуля
- `знімок_демо_[компанія].md`, `розбір_демо_[компанія].md` → `Read` на колі 2:
  що вже зібрано і що на демо зламалось

**Довідники:** `references/deal_journal_template.md`, `references/registers.md`.

**Збереження результату:**
- `план_демо-збірки_[назва_компанії].md` → `Write`

**Після завершення:**
- Повідом: «План демо-збірки [компанія] готовий: [n] процесів до показу,
  [k] свідомо не показуємо. **Прочитай і заперечи зараз** — після збірки правки
  дорожчі. Обіцянок із `P-N`, які треба показати: [m].»

---

## Місце в потоці

```
data-completeness-scorer + preliminary-estimate → коефіцієнт і вилка
    ↓
demo-build-plan  → план_демо-збірки_[компанія].md   ← ЦЕЙ СКІЛ
    ↓
СЕЙЛ ЧИТАЄ І ЗАПЕРЕЧУЄ (тут правки ще дешеві)
    ↓
S4: demo-base-builder → зібрана база на стенді
    ↓
demo-runbook → сценарій проведення (пишеться ПІСЛЯ збірки)
```

---

## Крок 0: Визнач вхідні дані

**Мінімум для роботи:** журнал із `G-N`.

**Якщо журналу немає або в ньому немає `G-N`:**
> «У журналі немає функціональних розривів — немає з чого будувати демо.
> Запусти **"розбери транскрипт брифу [компанія]"**, а якщо бриф уже розібраний —
> перевір, чи вимоги справді прозвучали на зустрічі.»

**Якщо `ata_need_demo` = no:** попередь — «У картці демо позначене як непотрібне.
Плануємо все одно?»

**Визнач коло.** Немає `питання_клієнта_[компанія].md` — це коло 1. Файл є — це коло 2
після першого демо, і тоді план **не переписується з нуля**: беремо попередній,
додаємо те, що клієнт просив показати, і прибираємо те, що вже зайшло. У документі
з'являється розділ «Що змінилось цього кола».

**Якщо коефіцієнт ×3:** це не блокер, але познач у документі:
> ⚠️ Коефіцієнт ×3: даних мало. Демо тут працює і як показ, і як спосіб дозбору —
> у сценарій варто закласти питання, а не лише показ.

---

## Крок 1: Вибери, що показуємо

Не все, що є в `G-N`. Демо має показати **те, заради чого клієнт прийшов**, і те,
що знімає його головне заперечення.

Пріоритет:

1. `G-N`, які прямо відповідають на болі з `D-N` — це ядро демо
2. `G-N`, на які є **обіцянка `P-N`** — якщо сейл сказав «це є в стандарті»,
   клієнт це шукатиме. Не показати — гірше, ніж не обіцяти
3. `G-N` категорій `стандарт` і `конфіг` — вони показуються дешево
4. `G-N` категорії `код` — показуємо прототипом у Studio, **якщо без цього демо
   не має сенсу**. Кожен такий екран потім рахується як розробка

**Чого не показуємо** — окремий розділ, і він обов'язковий:
- `X-N` — виключене зі scope
- `G-N` категорії `інтеграція`, якщо інтеграції немає на стенді: **не імітуємо
  обмін даними.** Показана «інтеграція», якої не існує, — це обіцянка, яку ми
  не давали свідомо
- усе, що потребує даних, яких у нас немає

---

## Крок 2: Опиши процеси мовою менеджера

Для кожного процесу до показу:

| Що описуємо | Як |
|---|---|
| Назва процесу | мовою клієнта: «приймання товару на склад», не «Inventory Receipts» |
| Хто його робить | роль, як вона називається в клієнта |
| Кроки | 3–7 кроків, дієсловами: «комірник сканує», «система пропонує» |
| На яких даних | які довідники й скільки записів потрібно, щоб виглядало правдоподібно |
| Що клієнт має побачити | результат, який знімає його біль |

**Без термінів Odoo там, де можна без них.** Якщо термін неминучий — поясни
в дужках при першій згадці.

> Перевірка: чи міг би сейл, прочитавши цей розділ, розповісти клієнту, що буде
> на демо, не відкриваючи Odoo? Якщо ні — розділ написаний не тією мовою.

---

## Крок 3: Демо-дані

База без правдоподібних даних не переконує. Опиши, що потрібно наповнити:

- довідники галузі: номенклатура, контрагенти, склади, одиниці — **скільки записів**
  і якого характеру
- чи потрібні реальні назви клієнта (асортимент, підрозділи) — і чи є вони в нас
- історія: чи потрібні минулі документи, щоб звіти не були порожні

**Не вигадуй чужі дані як реальні.** Демо-дані — це демо-дані; якщо клієнт дав свій
асортимент, це окремо і краще. Якщо не дав — так і напиши, і не подавай вигадану
номенклатуру як «ваші дані».

---

## Крок 4: Pre-save верифікація

- [ ] Назва файлу: `план_демо-збірки_[реальна назва компанії].md`
- [ ] Кожен процес до показу привʼязаний до `G-N` і до болю з `D-N`
- [ ] `G-N` з обіцянками `P-N` або показуються, або явно винесені в «не показуємо»
      з поясненням
- [ ] Розділ «Чого не показуємо» **не порожній** і містить `X-N`
- [ ] Інтеграції, яких немає на стенді, **не імітуються** — сказано прямо
- [ ] Studio-екрани перелічені окремо з приміткою «рахується як розробка»
- [ ] Процеси описані мовою клієнта; терміни Odoo пояснені або відсутні
- [ ] Демо-дані описані кількісно: скільки записів і якого характеру
- [ ] Вигадані дані не подані як дані клієнта
- [ ] Названо, що сейл мусить заперечити **зараз**, поки збірки немає
- [ ] Коло визначене; на колі 2 план **не переписаний з нуля** і містить розділ
      «Що змінилось цього кола»
- [ ] Коло 2: усе, що клієнт просив показати, або увійшло в план, або винесене
      в «Чого не показуємо» з причиною
- [ ] Усі `[⚠️ ІНСТРУКЦІЯ: …]` замінені

---

## Крок 5: Шаблон

```markdown
# План демо-збірки: [Назва компанії]

| | |
|---|---|
| Дата | [РРРР-ММ-ДД] |
| Коефіцієнт | [×N] |
| Процесів до показу | [n] |
| Стенд | [⚠️ ІНСТРУКЦІЯ: інстанс, на якому збираємо. НЕ прод.] |

Регістр: внутрішній людський. Клієнту не надсилається.

> **Прочитай і заперечи зараз.** Після збірки бази правки коштують дорожче.

---

## Що клієнт має винести з демо

[⚠️ ІНСТРУКЦІЯ: 2–3 речення. Головне заперечення, яке демо мусить зняти.]

## Процеси до показу

### [Назва процесу мовою клієнта]
| | |
|---|---|
| Закриває | [G-N + біль із D-N] |
| Хто робить | [роль] |
| Обіцянка на цю тему | [P-N або «немає»] |

**Кроки:** [⚠️ ІНСТРУКЦІЯ: 3–7 кроків дієсловами]
**Дані:** [⚠️ ІНСТРУКЦІЯ: що наповнити]
**Клієнт побачить:** [⚠️ ІНСТРУКЦІЯ: результат]

## Чого не показуємо і чому

| Що | Чому |
|----|------|

[⚠️ ІНСТРУКЦІЯ: X-N, відсутні інтеграції, брак даних. Розділ обов'язковий:
непоказане має бути свідомим рішенням, а не забутим.]

## Прототипи в Studio

| Екран | Що замінює | Рахується як |
|-------|-----------|--------------|

[⚠️ ІНСТРУКЦІЯ: усе тут — «розробка» в оцінці. Немає прототипів — прибери розділ.]

## Демо-дані

| Довідник | Скільки записів | Характер | Джерело |
|----------|-----------------|----------|---------|

## Що заперечити зараз

[⚠️ ІНСТРУКЦІЯ: 2–4 питання до сейла. Напр. «Показувати виробництво? У G-N воно є,
але даних по маршрутах немає — вийде поверхнево.»]

## Що змінилось цього кола

[⚠️ ІНСТРУКЦІЯ: коло 1 — прибери розділ. Коло 2 — що додали з питань клієнта,
що прибрали, бо вже зайшло. Посилайся на пакет питань.]
```

---

## Наступний крок

> «План демо-збірки [компанія] готовий. Далі:
> • **прочитай і заперечи** — розділ «Що заперечити зараз»;
> • коли план погоджений — **"збери демо-базу для [компанія]"** (`demo-base-builder`);
> • після збірки — **"склади план проведення демо для [компанія]"** (`demo-runbook`).»

---

## Важливі правила

**Мовою менеджера.** Якщо сейл не може переказати план клієнту, не відкриваючи Odoo, —
план написаний не для нього.

**Непоказане — свідоме рішення.** Розділ «Чого не показуємо» обов'язковий. Забуте
і свідомо виключене виглядають на демо однаково, а коштують по-різному.

**Інтеграції не імітуємо.** Показаний обмін даними, якого немає, — це обіцянка,
якої ми не давали. На дискавері за неї відповідатимуть інші люди.

**Обіцянки треба показувати.** Якщо в `P-N` є «це вже є в стандарті», клієнт це
шукатиме на демо. Або показуємо, або явно виносимо в «не показуємо» — і тоді сейл
знає, що з цим доведеться щось робити.

**Не вигадуй.** Немає даних клієнта — не подавай вигадану номенклатуру як його.
Немає підстави показувати блок — не показуй.

**Studio — це розробка.** Кожен прототипний екран іде в оцінку годинами розробки.
