---
name: presale-pack-builder
description: >
  Запускай перед демо, коли база зібрана і сценарій готовий, і сейлу потрібен пакет
  документів на руки: план презентації, функціональні розриви, оцінка проєкту, оцінка
  дискавері і КП. Пакет мішаний за регістрами — план презентації внутрішній,
  решта чотири клієнтські з обов'язковим Humizer. Тригерні фрази — "збери пакет
  до демо", "пакет документів", "п'ять документів до демо", "підготуй КП до демо",
  "карта розривів", "оцінка проєкту і дискавері". НЕ фінальний пакет після демо —
  його робить offer-finalizer; НЕ сценарій показу — його робить demo-runbook.
  Результат — пакет із п'яти документів: чотири клієнтські у **`.docx`** фірмовим
  стилем ToDo плюс план презентації.
---

# Presale Pack Builder

## Мета

Скласти пакет, з яким сейл заходить на демо повністю озброєним: він знає, що
показувати, що відповідати, скільки це коштує — і має КП, готове піти клієнту
в день зустрічі, якщо сейл вважатиме це доречним.

**Регістри виходів — пакет мішаний.** Це головна особливість скіла: регістр належить
документу, не скілу.

| Документ | Формат | Регістр | Humizer |
|----------|--------|---------|---------|
| План презентації | `.md` | **2 — внутрішній** | не застосовується |
| Функціональні розриви | **`.docx`** | **3 — клієнтський** | обов'язковий |
| Оцінка проєкту | **`.docx`** | **3 — клієнтський** | обов'язковий |
| Оцінка дискавері | **`.docx`** | **3 — клієнтський** | обов'язковий |
| КП | **`.docx`** | **3 — клієнтський** | обов'язковий |

**Чотири клієнтські документи віддаються у `.docx`, не в markdown.** Клієнт відкриває
їх у Word і читає як документи компанії — вигляд є частиною пропозиції. Стиль зашитий
у `scripts/make-docx.py` і знятий із реального КП ToDo; структура КП — теж зі зразка,
дослівно. Контракт: `references/docx_style.md`.

Поряд із кожним `.docx` лишається **чернетка-джерело** `_джерело.md`: із неї
`offer-finalizer` перевипускає версію, бо `.docx` парсити нічим.
| Розділ 13 журналу — чотири рядки версії `v1` | **1 — машиночитний** | заборонений |

План презентації в пакеті **не переписується** — його автор `demo-runbook`.
Тут він лише перелічений як частина пакета.

---

## Інструменти та середовище

**Читання вхідних файлів:**
- `журнал_продажу_[компанія].md` → `Read`. `G-N` (ядро карти розривів), коефіцієнт,
  `X-N`, відкриті `Q-N`, `P-N`
- `попередня_оцінка_[компанія].md` → `Read`. Вилка й аналоги
- `план_презентації_[компанія].md` → `Read`. Частина пакета, не переписуємо
- `знімок_демо_[компанія].md` → `Read`. Що реально працює

**Спільні конвенції — прочитай перед першим кроком:** `references/skill_conventions.md`
(мова за регістром · обсяг виходу · ідемпотентність · поведінка при збої · ліміти входу ·
фіксація погодження)

**Довідники — прочитай ТРИ:**
- `references/docx_style.md` — **розділи 1 (як складається документ), 2 (стиль),
  3 (розмітка), 4 (скелет КП), 5 (скелети трьох інших), 6 (Pre-save)**. Це головний
  довідник цього скіла
- `references/deal_journal_template.md` — розділ 5 «Категорії `G-N`» і правило Studio
- `references/registers.md` — розділи 3 «Humizer» і 4 «Фільтр чутливого»

**Складання `.docx` — через скрипти, не руками:**
- `python3 scripts/make-docx.py чернетка.md вихід.docx --title "…"` → `Bash`
- `python3 scripts/check-docx.py вихід.docx` → `Bash`. Девʼять перевірок стилю;
  **не пройшли — документ не віддається**

> **Шляхи скриптів і ресурсів — відносно каталогу цього скіла**, як у house-скілах.
> Інструментарій лежить усередині скіла (`scripts/`, `assets/`), бо встановлений
> плагін кореня репозиторію не бачить. Перед викликом перейди в каталог скіла
> або підставляй повний шлях до нього.

**Збереження результату:**
Кожен клієнтський документ — **два файли**: чернетка-джерело і зібраний `.docx`.

- `розриви_[назва_компанії]_v1_джерело.md` → `Write`, далі `make-docx.py` →
  `розриви_[назва_компанії]_v1.docx` (регістр 3)
- `оцінка_проєкту_[назва_компанії]_v1_джерело.md` → `.docx` тим самим шляхом
- `оцінка_дискавері_[назва_компанії]_v1_джерело.md` → `.docx`
- `КП_[назва_компанії]_v1_джерело.md` → `.docx`
- `журнал_продажу_[назва_компанії].md` → `Write`: **чотири рядки в розділ 13
  «Версії документів»**, версія `v1`, з датою і файлом

> **Суфікс `_v1` обов'язковий, і розділ 13 журналу заповнює саме цей скіл.**
> `offer-finalizer` визначає номер наступної версії як «максимальна наявна + 1»
> і читає розділ 13. Якщо пакет до демо не має версії й не внесений у журнал,
> та логіка не має від чого відштовхнутись.

**Після завершення:**
- Повідом: «Пакет до демо [компанія] зібраний: 4 документи `.docx` + план презентації.
  Humizer пройдено для всіх чотирьох клієнтських. Вилка: [від]–[до] € без ПДВ.
  Ліцензії Odoo: [внесені сейлом / **порожні — оцінка неповна**].
  **КП показуєш за власним рішенням і тільки якщо не спрацював жоден стоп-сигнал.**
  Перевірку стилю пройшли всі чотири; **відкрий перший документ у Word** — я перевірив
  структуру, але сторінку не бачив.»

---

## Місце в потоці

```
preliminary-estimate → вилка · demo-runbook → сценарій · demo-base-builder → знімок
    ↓
presale-pack-builder  → 4 клієнтські документи + план презентації   ← ЦЕЙ СКІЛ
    ↓
ДЕМО КЛІЄНТУ (КП — за рішенням сейла, за відсутності стоп-сигналів)
    ↓
demo-debrief (за умовою) · demo-followup-writer (завжди)
    ↓
S5: offer-finalizer → фінальний пакет
```

---

## Крок 0: Визнач вхідні дані

**Мінімум для роботи:** журнал із `G-N` і `попередня_оцінка_[компанія].md`.

**Якщо попередньої оцінки немає:**
> «Немає `попередня_оцінка_[компанія].md`. Без неї оцінка проєкту буде вигаданою.
> Запусти **"дай попередню оцінку по [компанія]"**.»

**Якщо рядок ліцензій Odoo у попередній оцінці порожній:** пакет збираємо, але
**у КП і в оцінці проєкту лишаємо цей рядок порожнім із приміткою** — і кажемо
про це в підсумку. Скіл ліцензії не вигадує і не тягне з прайсу.

**Якщо плану презентації немає:** попередь, що пакет неповний, і збери решту.
Не пиши сценарій сам — це `demo-runbook`.

### Порядок збірки і що робити при обриві

Чотири документи за один прогін — це довго, і прогін може обірватись на середині.
Тому порядок фіксований, від найменш залежного до найзалежнішого:

```
1. розриви         ← з G-N, нічого не потребує крім журналу
2. оцінка проєкту  ← потребує розривів (обсяг) і попередньої оцінки
3. оцінка дискавері ← потребує розривів
4. КП              ← потребує оцінки проєкту і оцінки дискавері
```

**Зберігай кожен документ одразу після Humizer, не всі в кінці.** Три збережені
документи кращі за чотири незбережені.

**Якщо прогін обірвався**, при повторному запуску:
1. подивись, які файли пакета вже є в папці, і **прочитай їх**;
2. готові не перезбирай — звір їх із журналом: якщо журнал змінився після їхньої
   дати, перезбирай саме ці;
3. у підсумку скажи, які документи цього прогону нові, які лишились із попереднього
   і чи вони узгоджені між собою.

**Головна перевірка узгодженості — цифри.** Вилка в КП мусить дослівно збігатись
з оцінкою проєкту, а обсяг у КП — зі списком розривів. Якщо КП зібране раніше,
ніж оновилась оцінка, у пакеті лежать **дві різні ціни**, і клієнт побачить обидві.
Перед завершенням звір суми в усіх чотирьох документах і скажи в підсумку:
«Вилка в КП і в оцінці проєкту збігається: [від]–[до] €.»

---

## Крок 1: Функціональні розриви — клієнтський документ

Ядро — `G-N` із журналу. Кожна вимога розкладена за категорією покриття з наслідком
у годинах.

| Категорія в журналі | Як звучить клієнту |
|---|---|
| `стандарт` | покривається штатною системою |
| `конфіг` | покривається налаштуванням |
| `код` | потребує доробки |
| `інтеграція` | обмін із зовнішньою системою |
| `поза скоупом` | у цей етап не входить |
| **`невідомо`** | **обсяг визначається на обстеженні** |

**Studio-прототип тут рахується як доробка**, не як налаштування. Це нижня межа
вилки, а не верхня.

**`невідомо` — не «доробка» і не викинутий рядок.** Категорія означає, що не ясно
навіть, налаштування це чи окремий проєкт (розділ 5 контракту журналу). У клієнтському
документі такий розрив:
- **присутній** — клієнт сам його підняв на зустрічі, і не побачити його в переліку
  означає, що ми його не почули;
- **без годин**, із формулюванням «обсяг визначається на обстеженні». Не «потребує
  доробки»: це завищить оцінку на блоці, глибини якого ми не знаємо;
- **обов'язково має пару** в списку уточнень, потрібних для точної оцінки — саме
  те `[КО]`, що його блокує.

Порахувати такий рядок як `код` — найгірший із варіантів: цифра поїде вгору,
і захищати її буде нічим.

**Що НЕ йде в цей документ:** коефіцієнт вилки і його механіка, `Q-N` як перелік
наших прогалин, `ST-N`, `R-N`. Відкриті питання подаються як **уточнення, потрібні
для точної оцінки**, і не більш ніж списком без нашої внутрішньої нумерації.

---

## Крок 2: Оцінка проєкту — клієнтський документ

Вилка від–до в годинах і грошах, по блоках. **Ставка — з розділу 3 журналу,
її ставить сейл**; ставок може бути дві (дискавері й впровадження). Сума в євро.
Немає ставки — документ віддає вилку **в годинах** і каже про це прямо.

**Показуємо підсумок, не механіку:**

| Правильно | Неправильно |
|---|---|
| `320–480 годин · 16 000–24 000 € без ПДВ` | `320 год × [ставка] €/год, верхня межа ×1,5 через неповноту даних` |

Ні ставки окремим рядком, ні коефіцієнта, ні способу розрахунку. Це фільтр
чутливого, розділ 4 `registers.md`.

Ліцензії Odoo — окремий рядок. Порожній, якщо сейл їх не внесла.

---

## Крок 3: Оцінка дискавері — клієнтський документ

**Звідки години.** Це єдиний документ пакета, для якого `preliminary-estimate`
не дає цифр: він оцінює проєкт, не дискавері. Порядок:

1. `G-N` категорії, які вимагають окремого обстеження (інтеграції, виробництво,
   мультиюрособовість) — з них складається перелік робіт дискавері;
2. **години вносить сейл**, як ліцензії і ставку. Скіл їх не вигадує;
3. немає годин — блок лишається з `уточнюється`, і про це сказано в підсумку.

> ⚠️ **Звір із тим, що вже назвали клієнту.** У живій практиці оцінку дискавері
> називають у першому ж листі-відповіді, до всякого пакета. Якщо в журналі є `P-N`
> типу «термін» або «ціна» з годинами дискавері — беремо **ту саму цифру**
> або явно пояснюємо, чому вона змінилась. Друга, інша цифра в пакеті — це готове
> заперечення на захисті.

Вилка з переліком робіт і **ролями**, які будуть задіяні.

**Ролі — так, конкретні люди — ні.** Склад команди на цьому етапі не визначений,
і обіцяти його не можна: це готовий `P-N` для когось іншого.

Потрібна, щоб перший рахунок на передплату виставлявся без окремого раунду
погоджень.

---

## Крок 4: КП — клієнтський документ

Готується **заздалегідь**, щоб могло піти клієнту в день зустрічі.

Показувати його на демо — **рішення сейла**. Але:

> **Спрацював хоча б один стоп-сигнал — КП не дістаємо і не надсилаємо.**
> Це не рекомендація. Стоп-сигнал знімає рішення сейла.

Це має бути написано в підсумку скіла і в шапці КП як внутрішня примітка,
яку сейл видаляє перед відправкою.

Склад: обсяг робіт, вилка в євро, місце під ліцензії, умови за Time&Material,
терміни з конкретними датами.

---

## Крок 5: Humizer для чотирьох клієнтських

Обов'язковий і **блокуючий**. За розділом 3 `references/registers.md`.

**Чотири документи — чотири окремі проходи.** «Humizer пройдено для пакета» не
існує: у документах різна мова і різні пастки, і пройдений розрив не робить чистим КП.

| Документ | Де ловиться найчастіше саме тут |
|---|---|
| розриви | ID у кожному рядку, категорії `конфіг`/`код` як є, `X-N` подані як «ми виключили» |
| оцінка проєкту | коефіцієнт і ставка, «вилка ×3», години як внутрішня одиниця |
| оцінка дискавері | імена конкретних людей замість ролей, внутрішні назви етапів |
| КП | залишки механіки в обґрунтуванні ціни, `P-N` як цитати, білі плями як перелік того, чого ми не знаємо |

Порядок проходу: **чернетка → Humizer → `.docx` → перевірка стилю**. Не збирай усі
чотири й не проходь Humizer «оптом» у кінці: до кінця прогону формулювання з першого
документа вже здаються нормальними, і саме так ID доїжджають до клієнта.

**Humizer проходить по чернетці `.md`, а не по `.docx`.** Причина практична: у `.docx`
текст порізаний на прогони, і вичитувати його нічим. Чернетка — те, що читає людина
й перевіряє скіл; `.docx` — те, що з неї збирається.

Після кожного проходу — рядок `Humizer пройдено` у шапці **цього** документа.
У підсумку скіла — чотири рядки, по одному на документ, а не один спільний.

Тут ловиться найчастіше:

- **усі ID**: `G-12`, `Q-7`, `X-3`, `P-1`, теги `[КО]`/`[ВО]`/`[ПП]`
- жаргон: scope → обсяг робіт · As-Is/To-Be → поточний / цільовий стан ·
  master data → основний довідник · BoM/MO → специфікація / замовлення на виробництво
- **ставка, коефіцієнт, механіка розрахунку**
- `ST-N`, `R-N`, білі плями як перелік прогалин
- назви інших клієнтів

Зберегти точно: дати, суми, обсяги, домовленості.

---

## Крок 5а: Складання `.docx`

Humizer пройдено, чернетки готові — тепер із кожної збирається `.docx`. Порядок
для кожного документа окремо, **не оптом**:

```bash
python3 scripts/make-docx.py розриви_[компанія]_v1_джерело.md \
        розриви_[компанія]_v1.docx --title "Функціональні розриви: [компанія]"
python3 scripts/check-docx.py розриви_[компанія]_v1.docx
```

**Розмітка чернетки — підмножина markdown** із `references/docx_style.md` §3:
`#` титул · `##` розділ (оранжевий, центр) · `###` підрозділ · `####` і `#####`
глибше · `-` маркери · `|` таблиці · `>` виноска · `---` лінія · `[[LOGO]]` ·
`[[PAGEBREAK]]`. Скрипт **не приймає нічого іншого** і падає з номером рядка —
це навмисно: тиха втрата розмітки в клієнтському документі дорожча за помилку
в консолі.

**Якщо `check-docx.py` дав хоч один `✗`** — документ **не віддається**. Виправ
чернетку і перезбери; правити `.docx` руками не можна, бо наступний перевипуск
його перезапише.

**Візуального рендеру в скіла немає** — LibreOffice у нашому середовищі не завантажує
файли, тому перевірка структурна. Тому в підсумку окремим рядком:
> «Перший документ по цьому клієнту **відкрий у Word** перед відправкою — я перевірив
> структуру, але не бачив сторінку.»

**Ліцензії Odoo, ставка й курс** у `.docx` не з'являються самі: ліцензії вносить сейл,
ставка живе в журналі й у клієнтський документ не йде взагалі (тільки сума).

---

## Крок 6: Pre-save верифікація

**Спільне:**
- [ ] `G-N` категорії **`невідомо`** у переліку розривів присутні, **без годин**,
      із формулюванням «обсяг визначається на обстеженні», і кожен має пару
      в списку уточнень. Як `код` не порахований
- [ ] Чотири клієнтські документи є **у двох файлах кожен**: чернетка `_джерело.md`
      і зібраний `.docx`; план презентації **не переписаний**
- [ ] `make-docx.py` пройшов без помилок для всіх чотирьох
- [ ] `check-docx.py` — **девʼять перевірок зелені** на кожному `.docx`
- [ ] Humizer пройдено **по чернетці**, до складання `.docx`
- [ ] У чернетках немає попереджень для сейла і взагалі нічого внутрішнього:
      усе, що в чернетці, потрапляє в документ клієнта
- [ ] У КП порядок розділів **зі скелета** `docx_style.md` §4; жоден не пропущений
- [ ] У таблиці вартості є рядок **«Разом по всім етапам»**; у КП є рядок
      **«Пропозиція дійсна до»** з датою
- [ ] Ліцензії Odoo — окремий рядок; порожній, якщо сейл не внесла
- [ ] У підсумку сказано **відкрити перший документ у Word**: скіл структуру
      перевірив, сторінку не бачив
- [ ] У кожному клієнтському документі шапка з регістром і приміткою для сейла
- [ ] **Humizer пройдено окремим проходом для кожного** з чотирьох клієнтських;
      у шапці кожного є свій рядок, у підсумку — чотири рядки, не один
- [ ] Кожен документ збережений одразу після свого Humizer, не «всі в кінці»
- [ ] Вилка в КП **дослівно збігається** з оцінкою проєкту; обсяг у КП — зі списком
      розривів. Дві різні ціни в пакеті — блокуючий дефект
- [ ] Якщо частина документів лишилась із попереднього прогону — сказано, які саме
      і чи вони узгоджені з журналом на сьогодні
- [ ] У клієнтських документах **не лишилось жодного ID** і жодного тега
- [ ] Немає ставки, коефіцієнта, механіки розрахунку, `ST-N`, `R-N`
- [ ] Немає назв інших клієнтів

**Розриви:**
- [ ] Кожна вимога з `G-N` присутня; категорії перекладені клієнтською мовою
- [ ] Studio-прототипи рахуються як **доробка**
- [ ] `X-N` подані як «у цей етап не входить», не як наші виключення

**Оцінка проєкту:**
- [ ] Підсумок без механіки; ставка окремим рядком **відсутня**
- [ ] Ліцензії Odoo — окремий рядок; порожній, якщо сейл не внесла
- [ ] Сума в євро без ПДВ

**Оцінка дискавері:**
- [ ] Ролі названі, **конкретні люди — ні**

**КП:**
- [ ] Внутрішня примітка про стоп-сигнали є і позначена як «видалити перед відправкою»
- [ ] Терміни — конкретні дати, не «найближчим часом»
- [ ] Є рядок **«Пропозиція дійсна до»** з конкретною датою
- [ ] Є розділ **«Що не входить у цей етап»** — з `X-N`, поданих як межа етапу
- [ ] Дати, що залежать від клієнта, подані умовою («за умови доступу до [дата]»)
- [ ] **Обсяг у межах §3 конвенцій**: клієнтські документи таблицями; понад 30 рядків — групуємо
- [ ] **Мова за §2 конвенцій**: **мова клієнта** в усіх чотирьох (регістр 3); план презентації — мовою сейла. Одна мова на документ; цитати — мовою оригіналу
- [ ] **Повторний прогін за §4 конвенцій**: джерело звірене з шапкою, правки людини
      не затерті, статуси оновлені замість дублювання рядків
- [ ] Усі `[⚠️ ІНСТРУКЦІЯ: …]` замінені в усіх файлах

---

## Крок 7: Шаблони чернеток

**Скелети всіх чотирьох документів живуть у `references/docx_style.md`** — §4 для КП
і §5 для розривів, оцінки проєкту й оцінки дискавері. Тут вони **не дублюються
свідомо**: дві копії одного скелета розійдуться, і клієнт отримає документ
із чужою структурою.

Порядок роботи з кожним документом:

1. відкрий відповідний скелет у `references/docx_style.md`;
2. перенеси його в чернетку `_v1_джерело.md` **без переставляння розділів**;
3. заповни змістом із журналу, оцінки й знімка демо;
4. три стабільні блоки — не пиши, а вставляй:

```markdown
[[INCLUDE:assets/docx-boilerplate/про-odoo.md]]
[[INCLUDE:assets/docx-boilerplate/про-todo.md]]
[[INCLUDE:assets/docx-boilerplate/контакти.md]]
```

**Що специфічне саме для першого пакета (`v1`), і чого немає у скелеті:**

| Місце | Що ставимо в `v1` |
|---|---|
| версія в шапці кожного документа | `версія 1`; розділу «Що змінилось проти версії N-1» у `v1` **немає** |
| ліцензії Odoo | `[⚠️ ІНСТРУКЦІЯ: внесене сейлом або «уточнюється»]` — скіл їх не вигадує |
| вартість послуг | вилка з `попередня_оцінка`, **дослівно**; рядок «Разом по всім етапам» обов'язковий |
| «Пропозиція дійсна до» | `[⚠️ ІНСТРУКЦІЯ: конкретна дата; за замовчуванням 30 днів від дати]` |
| «Що не входить у цей етап» | з `X-N`, як межа етапу; розділ обов'язковий у КП і в розривах |
| «Що уточнюється на Discovery» | з відкритих `Q-N`, **без ID і без тегів** |
| контакти | імʼя, посаду й робочі контакти сейла пише скіл рядками **перед** вставкою `контакти.md` |

**Три блоки не пишуться, а вставляються.** «Про Odoo», «Про компанію ToDo»
і контакти лежать у `assets/docx-boilerplate/`. Причина в `references/docx_style.md` §7:
у них цифри — факти про продукт і компанію (600+ у R&D, 53 000 додатків,
65+ співробітників, 40+ упроваджень, третє місце в рейтингу партнерів). Переказ таких
цифр своїми словами — це округлення або тихе завищення, і клієнт може їх перевірити.

**Контакти конкретної людини пише скіл**, не boilerplate: у `контакти.md` тільки
загальні контакти компанії. Персональні дані співробітників у репозиторії
не зберігаються — те саме правило, що в досьє передачі.

**Попередження про стоп-сигнали в чернетку не йде.** Усе, що в ній є, потрапляє
в `.docx`, який читає клієнт. Тому рядок «показувати КП — рішення сейла, і стоп-сигнал
його знімає» звучить **у підсумку скіла**, а не у файлі.

---


## Наступний крок

> «Пакет до демо [компанія] зібраний. Далі:
> • **проведи демо** за планом презентації; КП — за власним рішенням і **тільки
>   за відсутності стоп-сигналів**;
> • після демо завжди — **"напиши лист після демо для [компанія]"**
>   (`demo-followup-writer`);
> • якщо спрацював стоп-сигнал без показаної оцінки або ти пообіцяв зміни —
>   **"розбери демо [компанія]"** (`demo-debrief`);
> • якщо призначено повторну зустріч — **"збери питання клієнта по [компанія]"**
>   (`client-questions-pack`).»

---

## Важливі правила

**Оновлюй журнал на місці й продовжуй ID.** Цей скіл пише в журнал один розділ —
13 «Версії документів», — але правило те саме, що для решти writers: чотири рядки
**додаються** до наявних, попередні версії не затираються, і жоден `D-/X-/G-/Q-/C-/P-/ST-/R-/A-`
не перенумеровується. Продовжуй ID, не перенумеровуй: на розділ 13 спирається
`offer-finalizer`, коли визначає номер наступної версії.

**Регістр належить документу.** Один скіл, два регістри. План презентації внутрішній,
чотири інші клієнтські — і Humizer для них блокуючий.

**Підсумок, не механіка.** Клієнт бачить вилку. Ставку, коефіцієнт і спосіб
розрахунку — ні, навіть як пояснення, навіть як відкрите питання.

**Ліцензії вносить сейл.** Порожній рядок із «уточнюється» — правильний стан.
Скіл їх не вигадує і не тягне з прайсу.

**Studio — це доробка.** У карті розривів і в оцінці кожен прототипний екран
рахується годинами розробки.

**Ролі так, люди ні.** В оцінці дискавері склад команди не обіцяємо: це готова
обіцянка, за яку відповідатимуть інші.

**Стоп-сигнал сильніший за рішення сейла.** КП готове заздалегідь, але спрацював
сигнал — не дістаємо. Внутрішня примітка в КП про це є.

**Не вигадуй.** Немає підстави для цифри — блок лишається з приміткою «уточнюється».
Немає вимоги в журналі — її немає і в карті розривів.
